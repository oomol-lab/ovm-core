patches="libcap-2.69"

config() {
	sed -i '/install -m.*STA/d' libcap/Makefile
}

build() {
	export CROSS_COMPILE="$TGT-"

	make prefix=/usr lib=lib -j$(nproc) BUILD_CFLAGS="-O2 -I/usr/include" BUILD_LDFLAGS="-L/lib -L/usr/lib/x86_64-linux-gnu" BUILD_CC=gcc LD="${CC}" LDFLAGS="${LDFLAGS}"
}

