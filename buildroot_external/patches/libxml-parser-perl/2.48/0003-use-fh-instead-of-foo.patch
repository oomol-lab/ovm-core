From 35228679f2a79004b3b9e46372d6c6690a1ecaa5 Mon Sep 17 00:00:00 2001
From: Graham Ollis <plicease@cpan.org>
Date: Sat, 27 Jun 2020 23:23:54 -0600
Subject: [PATCH] use $fh instead of $foo

---
 Parser.pm | 4 ++--
 README    | 4 ++--
 README.md | 4 ++--
 3 files changed, 6 insertions(+), 6 deletions(-)

diff --git a/Parser.pm b/Parser.pm
index 20e6aab..2870b69 100644
--- a/Parser.pm
+++ b/Parser.pm
@@ -346,8 +346,8 @@ XML::Parser - A perl module for parsing XML documents
                    Default => \&other);
 
   open(my $fh, 'xmlgenerator |');
-  $p3->parse($foo, ProtocolEncoding => 'ISO-8859-1');
-  close($foo);
+  $p3->parse($fh, ProtocolEncoding => 'ISO-8859-1');
+  close($fh);
 
   $p3->parsefile('junk.xml', ErrorContext => 3);
 
diff --git a/README b/README
index e2cf90f..4e363b1 100644
--- a/README
+++ b/README
@@ -21,8 +21,8 @@ SYNOPSIS
                        Default => \&other);
 
       open(my $fh, 'xmlgenerator |');
-      $p3->parse($foo, ProtocolEncoding => 'ISO-8859-1');
-      close($foo);
+      $p3->parse($fh, ProtocolEncoding => 'ISO-8859-1');
+      close($fh);
 
       $p3->parsefile('junk.xml', ErrorContext => 3);
 
diff --git a/README.md b/README.md
index e6e5425..ae8575c 100644
--- a/README.md
+++ b/README.md
@@ -24,8 +24,8 @@ XML::Parser - A perl module for parsing XML documents
                      Default => \&other);
 
     open(my $fh, 'xmlgenerator |');
-    $p3->parse($foo, ProtocolEncoding => 'ISO-8859-1');
-    close($foo);
+    $p3->parse($fh, ProtocolEncoding => 'ISO-8859-1');
+    close($fh);
 
     $p3->parsefile('junk.xml', ErrorContext => 3);
 
